  <label id="{{ $dato->name }}data" data="{{$dato->dato}}">{{$dato->dato}}
</label>
<input type="button" name="{{ $dato->name }}" id="{{ $dato->id }}" class="boton01" value="Editar">
<br>

<div class="mensaje">{{ $dato->mensaje }}</div>
